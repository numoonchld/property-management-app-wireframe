import React, { Fragment } from "react";
import { Link } from "react-router-dom";

const Navbar = () => {
  return (
    <Fragment>
      <nav className="navbar navbar-dark bg-dark text-light"></nav>
    </Fragment>
  );
};

export default Navbar;
